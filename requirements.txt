# Web framework for creating API endpoints and handling HTTP requests
Flask>=2.2.2

# LangChain for managing prompts, memory, and interaction with LLMs (Groq API)
langchain>=0.0.170

# HTTP requests library (if needed for Groq API calls inside LangChain or custom code)
requests>=2.28.1

# For environment variable management (optional but recommended)
python-dotenv>=1.0.0

# Enable modern async features and faster server (optional enhancement if you consider FastAPI later)
# fastapi>=0.98.0
# uvicorn>=0.21.1

# Note: Frontend technologies (JavaScript, HTML, CSS) are static files, so they don’t go in requirements.txt

